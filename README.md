#file hello.py
from tkinter import *
#creating main window
root = Tk()
#creating a label widget
myLabel - Label(root, text = "Hello World")
#put the label into the window myLabel. pack()
#start the event loop root.mainloop()
